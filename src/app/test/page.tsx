export default function test() {

    // This is a test page.
    // My Simple Project is helped by Chat GPT. But it's faster for me to do it.
    // Because it's a moron than I thought. GPT is such an asshole!@#$%^&*()
    return (
        <div>
            test
        </div>
    );
}